# Email Configuration (LOCAL DEVELOPMENT ONLY)
# In production, the contact form uses Cloudflare Worker with Resend
# Replace with your actual SMTP credentials
SMTP_HOST=your-smtp-server.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-app-password
CONTACT_EMAIL_FROM=your-email@example.com
CONTACT_EMAIL_TO=your-email@example.com