---
const currentYear = new Date().getFullYear()

const links = {
  servicios: [
    { name: 'Redacción SEO', href: '/servicios' },
    { name: 'Copywriting', href: '/servicios' },
    { name: 'Contenido web', href: '/servicios' },
    { name: 'Blog posts', href: '/servicios' },
  ],
  empresa: [
    { name: 'Sobre mí', href: '/sobre-mi' },
    { name: 'Portfolio', href: '/portfolio' },
    { name: 'Blog', href: '/blog' },
    { name: 'Contacto', href: '/contacto' },
  ],
  social: [
    { name: 'LinkedIn', href: '#', external: true },
    { name: 'Twitter', href: '#', external: true },
    { name: 'Instagram', href: '#', external: true },
  ]
}
---

<footer class="bg-gray-900">
  <div class="max-w-7xl mx-auto py-12 px-4 sm:px-6 lg:py-16 lg:px-8">
    <div class="xl:grid xl:grid-cols-3 xl:gap-8">
      <div class="space-y-8 xl:col-span-1">
        <div>
          <h3 class="text-2xl font-bold text-white">RedactorSEO</h3>
          <p class="mt-4 text-gray-300 text-base">
            Impulso tu presencia online con contenido optimizado para SEO que atrae, convierte y posiciona tu marca en los primeros resultados.
          </p>
        </div>
        <div class="flex space-x-6">
          {links.social.map((item) => (
            <a href={item.href} class="text-gray-400 hover:text-gray-300 transition-colors">
              <span class="sr-only">{item.name}</span>
              <svg class="h-6 w-6" fill="currentColor" viewBox="0 0 24 24">
                <!-- Icon placeholder -->
                <circle cx="12" cy="12" r="10"/>
              </svg>
            </a>
          ))}
        </div>
      </div>
      
      <div class="mt-12 grid grid-cols-2 gap-8 xl:mt-0 xl:col-span-2">
        <div class="md:grid md:grid-cols-2 md:gap-8">
          <div>
            <h3 class="text-sm font-semibold text-gray-400 tracking-wider uppercase">Servicios</h3>
            <ul class="mt-4 space-y-4">
              {links.servicios.map((item) => (
                <li>
                  <a href={item.href} class="text-base text-gray-300 hover:text-white transition-colors">
                    {item.name}
                  </a>
                </li>
              ))}
            </ul>
          </div>
          
          <div class="mt-12 md:mt-0">
            <h3 class="text-sm font-semibold text-gray-400 tracking-wider uppercase">Empresa</h3>
            <ul class="mt-4 space-y-4">
              {links.empresa.map((item) => (
                <li>
                  <a href={item.href} class="text-base text-gray-300 hover:text-white transition-colors">
                    {item.name}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </div>
    
    <div class="mt-12 border-t border-gray-700 pt-8">
      <p class="text-base text-gray-400 xl:text-center">
        &copy; {currentYear} RedactorSEO. Todos los derechos reservados.
      </p>
    </div>
  </div>
</footer>